apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: spark-operator-webhook-service
  labels:
    app.kubernetes.io/name: spark-operator
  name: webhook-service
  namespace: system
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: webhook-server
    app.kubernetes.io/name: spark-operator
